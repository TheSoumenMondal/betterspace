services:
  postgresdb:
    image: pgvector/pgvector:pg17
    ports: 
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: betterspace_local_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
    postgres_data: